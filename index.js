const lengthEl = document.getElementById("text-length");
const volumeEl = document.getElementById("text-volume");
const massEl = document.getElementById("text-mass");
const inputEl = document.getElementById("to-convert");
inputEl.addEventListener("keyup", function () {});
